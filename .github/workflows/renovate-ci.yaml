name: Renovate config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'renovate.json5?'
      - '.github/renovate.json5?'
      - '.gitlab/renovate.json5?'
      - '.renovaterc'
      - '.renovaterc.json5?'
      - 'package.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@4d930029f0206048449879800cb984ec6b0e9b8f # main
